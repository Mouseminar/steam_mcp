[build]
builder = "nixpacks"

[deploy]
startCommand = "python mcp_server.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[healthcheck]
enabled = true
path = "/mcp/health"
timeout = 100
interval = 10
