edition: 3.0.0
name: steam
services:
  modest-steam:
    component: fc3
    actions:
      pre-deploy:
        - run: pip install -r requirements.txt
          path: /kaniko/tmp/workspace   # 👈 强制在正确目录执行
    props:
      region: cn-hangzhou
      runtime: python3.10
      functionName: modest-steam
      code: ./
      handler: handler.handler
      timeout: 60
      memorySize: 512
